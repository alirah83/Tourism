
import React from 'react'
import '../css/style.css';

const TourCardFront = (props) => {
    
    return (
        <React.Fragment>
            {props.children}
        </React.Fragment>
    )
}

export default TourCardFront