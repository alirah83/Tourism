
import React from 'react'
import '../css/style.css';

const TourCardBack = (props) => {
    return (
        <React.Fragment>
            {props.children}
        </React.Fragment>
    )
}

export default TourCardBack